# python
.venv/
__pycache__/
*.pyc

# local MySQL instance + extracted dumps (regenerable from the committed tarball)
db/mysql_data/
db/*.sql
db/*.sock
db/*.log
db/mysqld.pid
# NOTE: db/alkabor.tar.gz (the ~12MB source DB) IS committed for reproducibility.

# third-party quest scripts (clone from EQMacEmu/quests; see README)
quests/

# dev screenshots, server log, scratch
analysis/*.png
server/server.log

# semantic-extraction batch INPUTS (regenerable); out_*.json results are kept
analysis/dialog_sem/in_*.json

# binary intermediate
*.pkl
